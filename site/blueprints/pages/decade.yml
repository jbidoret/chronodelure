title: Décennie

options:
  changeSlug: false
  # changeTitle: false
  duplicate: false
  delete: false
  # changeStatus: false

columns:
  - width: 2/3
    sections:
      meta:
        type: fields
        fields:
          subtitle:
            label: Sous-titre
            type: text
          introduction:
            type: markdown
            extends: fields/md
            label: Introduction
      years:
        type: pages
        headline: Années
        template: session
        layout: cardlets
        info: "{{page.year}}"
        
  - width: 1/3
    sections:
      files:
        headline: Fichiers
        type: files
      # data:
      #   type: fields
      #   fields:
      #     contributors:
      #       type: markdown
      #       extends: fields/minimal
      #       label: Contributeurs
      # stories:
      #   headline: Souvenirs
      #   type: pages
      #   templates: 
      #     - story
        